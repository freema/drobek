/**
 * /auth/google/callback — client half of the route module. All logic lives in
 * ./auth.google.callback.server.ts; the route only ever 302s.
 */
export default function AuthGoogleCallback() {
  return null;
}
