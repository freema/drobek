{
  "name": "@drobek/skills-check",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test-only (NSO-308): the repo gate of the agent skills skill_info serves (skills/<name>/SKILL.md + modules/*/SKILL.md) — runs the checkSkill library of @drobek/modules/testing (the 5-section format, every code example compiled and typechecked against the live SDK types) over the built-in modules and the general skills.",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@drobek/agent-dx": "workspace:*",
    "@drobek/core": "workspace:*",
    "@drobek/modules": "workspace:*",
    "@types/node": "^22",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "drobek-module-auth": "workspace:*",
    "drobek-module-data": "workspace:*",
    "drobek-module-email": "workspace:*",
    "drobek-module-files": "workspace:*",
    "drobek-module-forms": "workspace:*",
    "drobek-module-proxy": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
