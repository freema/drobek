// Thin route glue (M1b, PHY-121) — logic lives in @drobek/dashboard.
export { loader } from '@drobek/dashboard/routes/workspaces.$slug.apps.$appSlug.data.server';
export { default, meta } from '@drobek/dashboard/routes/workspaces.$slug.apps.$appSlug.data';
