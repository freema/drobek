// Thin route glue (U5, PHY-71) — logic lives in @drobek/oauth.
export { action, loader } from '@drobek/oauth/routes/oauth.authorize.server';
export { default, meta } from '@drobek/oauth/routes/oauth.authorize';
