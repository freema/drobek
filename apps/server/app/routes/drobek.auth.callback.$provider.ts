// Thin route glue (NSO-348) — the end-user sign-in providers' IdP callback; logic in @drobek/dashboard + @drobek/modules (+ the auth module).
export { action, loader } from '@drobek/dashboard/routes/end-user-callback.server';
