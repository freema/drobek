// Thin route glue (M1-02) — the owner signs every end user of an app out; the logic lives in @drobek/dashboard.
export { action, loader } from '@drobek/dashboard/routes/api.apps.$id.end-user-sessions.revoke.server';
