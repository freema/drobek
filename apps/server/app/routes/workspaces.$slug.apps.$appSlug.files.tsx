// Thin route glue (M2-01, NSO-288) — logic lives in @drobek/dashboard.
export { loader } from '@drobek/dashboard/routes/workspaces.$slug.apps.$appSlug.files.server';
export { default, meta } from '@drobek/dashboard/routes/workspaces.$slug.apps.$appSlug.files';
