// Thin route glue (M1-01) — the pending module-change decision API lives in @drobek/dashboard.
export { action, loader } from '@drobek/dashboard/routes/api.apps.$id.modules.$module.$decision.server';
