// Thin route glue (M2-04, NSO-284) — logic lives in @drobek/dashboard + @drobek/oauth.
export { action, loader } from '@drobek/dashboard/routes/me.connections.server';
export { default, meta } from '@drobek/dashboard/routes/me.connections';
