// Thin route glue (M2-03, NSO-301) — Forms tab (submissions); logic lives in @drobek/dashboard.
export { action, loader } from '@drobek/dashboard/routes/workspaces.$slug.apps.$appSlug.forms.server';
export { default, meta } from '@drobek/dashboard/routes/workspaces.$slug.apps.$appSlug.forms';
