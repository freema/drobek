// Thin route glue (M2-03, NSO-301) — Uploads tab (files module); logic lives in @drobek/dashboard.
export { action, loader } from '@drobek/dashboard/routes/workspaces.$slug.apps.$appSlug.uploads.server';
export { default, meta } from '@drobek/dashboard/routes/workspaces.$slug.apps.$appSlug.uploads';
